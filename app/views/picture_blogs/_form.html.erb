<%= form_with(model: picture_blog, local: true, url:choose_new_or_edit) do |form| %>
  <% if picture_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(picture_blog.errors.count, "error") %> prohibited this picture_blog from being saved:</h2>

      <ul>
      <% picture_blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input_area">
    <%= form.label :content %>
    <%= form.text_area :content, placeholder: "What are you doing?" %>
    <%= form.label :image %>
    <%= image_tag(@picture.image.url) if @picture.image && @picture.image.url %>
    <%= form.file_field :image %>
    <%= form.hidden_field :image_cache %>
    <br>
    <%= form.submit 'create new post', class: 'button' %>
  </div>
<% end %>
